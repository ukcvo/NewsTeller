SELECT ?event (AVG(?count) AS ?number)
WHERE
{
    SELECT ?event (COUNT(?link) AS ?count)
    WHERE
    {
        VALUES ?event { *keys* } .
        ?event sem:hasPlace ?entity .
        OPTIONAL { ?entity ?link <http://dbpedia.org/ontology/Place> }
    }
    GROUP BY ?entity ?event
}
GROUP BY ?event