SELECT ?entity ?entityDbpediaLabel 
WHERE
{
	VALUES ?entity { *keys* } .
	?entity skos:prefLabel ?prefLabel .
	?entity rdf:type* ?class . 
	GRAPH <http://www.newsreader-project.eu/modules/dbpedia-en> { ?class rdfs:label ?entityDbpediaLabel }
}
GROUP BY ?entity ?entityDbpediaLabel